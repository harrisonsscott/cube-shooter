public static class GlobalVariables {
    public static bool isAlive; // is the player alive,if false then nothing happens
    public static int currentRow; // how many rows the player has cleared, makes the blocks gradually stronger
}