using UnityEngine;

public static class Constants {
    public static float gravity = 0.3f; // how fast everything falls per second
    public static bool debugMode = true; // makes the player overpowered (only in editor)
}